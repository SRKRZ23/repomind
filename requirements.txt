# Core (no GPU required for these)
gitpython>=3.1
tree-sitter>=0.22
tree-sitter-languages>=1.10
tiktoken>=0.7
pydantic>=2.6

# UI
gradio>=4.40

# Server-side (MI300X — install on AMD Cloud only)
# vllm>=0.6  --extra-index-url https://download.pytorch.org/whl/rocm6.2
# torch>=2.4
# transformers>=4.45

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

# Benchmarks (only needed on droplet for plotting after the run)
matplotlib>=3.7
